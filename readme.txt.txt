A pokemon app created to illustrate the working principle of stack data structure using UI and views in android.

This application has some col features, these are:

1) Setting up remainder, that works exactlt like a notification with the time picker enabled, so the user can select
time manually through time picker, and the notification will be prompted at that specific timimg.

2) Stack based Push, Pop, Peek are implemented through Pokemon examles.